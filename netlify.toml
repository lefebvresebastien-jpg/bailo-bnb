[build]
  functions = "netlify/functions"
  edge_functions = "netlify/edge-functions"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"

# Rappels automatiques chaque matin à 8h
[functions."bnb-rappels"]
  schedule = "0 8 * * *"
